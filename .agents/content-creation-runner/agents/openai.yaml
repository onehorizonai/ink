interface:
  display_name: "Content Creation Runner"
  short_description: "Turn approved Ink ideas into review drafts"
  default_prompt: "Use $content-creation-runner to turn planned `[Ink Idea]` records into `[Ink Draft]` initiatives in `In Review`. Repair channel parentage first. If blocked, comment with the planned-blocker format and set the idea to `In Review`; never leave human-action items in `Planned`."
