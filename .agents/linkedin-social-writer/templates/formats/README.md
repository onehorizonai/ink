# Format Templates

Add reusable LinkedIn post formats here.

Each file should define:

- when to use the format
- the rough structure
- what makes the format work
- what to avoid

The store and finalize workflows can record which format template was used in the saved post metadata.
